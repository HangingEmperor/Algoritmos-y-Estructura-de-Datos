/*package Clase.Ejemplos.Listas;

public class Lista<T> {

    Nodo start;
    int tamano;

    private void insertaInicio(T data) {
        Nodo n = new Nodo();
        n.info = data;
    }

    private void insertaFin(T data) {

    }

    private void mostrarLista() {

    }

    private void eliminarUltimo() {
        if (inicio.sig == null)`
    }

    void eliminarUltimo() {
        if (primero.sgte == null) {
            primero = primero.sgte;
        } else {
            Nodo temp = primero.sgte;
            Nodo aux = primero;
            while (temp.sgte != null) {
                temp = temp.sgte;
                aux = aux.sgte;
            }
            aux.sgte = null;
        }
    }

    private void eliminaFin() {

    }
}
*/